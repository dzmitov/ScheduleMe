# ПРАВИЛА ЭКОНОМИИ (TOKEN SAVER)
1. Всегда старайся минимизировать количество токенов. Не читай файлы целиком, если задача локальная.
2. Перед любым изменением кода делай краткий "Plan" и запрашивай подтверждение (как Approval в SAP).
3. Если контекст задачи раздувается, предлагай начать "New Task".

# ТЕХНИЧЕСКИЙ КОНТЕКСТ (STAY ON TRACK)
1. Стек: Vite + React + TypeScript + Clerk + Vercel Postgres.
2. Для переменных окружения ВСЕГДА используй `import.meta.env` (не process.env).
3. Ролевая модель: Только 'dzmitov@gmail.com' является админом. Все проверки прав делать по этому email.